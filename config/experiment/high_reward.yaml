# @package _global_
defaults:
  - override /robot: basic_v1
  - override /model: ppo
  - override /maze: generated_6x6

model:
  policy_kwargs:
    net_arch: [256, 64, 256]

training:
  total_timesteps: 20_000_000

rewards:
  - type: "ExploreReward"
    weight: 10
  - type: "WallCollisionPenalty"
    weight: 0.1